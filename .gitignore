# env
.env
.venv/
venv/

# python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# notebook outputs (keep notebooks clean for git diffs)
# students will regenerate outputs locally

# OS / editor
.DS_Store
.idea/
.vscode/

# data — keep folder structure, not student PDFs / audio
data/sample_pdfs/*
!data/sample_pdfs/.gitkeep
data/audio_samples/*
!data/audio_samples/.gitkeep
data/video_samples/*
!data/video_samples/.gitkeep

# model cache
.cache/
*.pt
*.bin

# eval outputs and user-supplied ground truth
evals/reports/
evals/*/ground_truth.json
# Phase 3 ground truth is committed (self-contained, references no user data)
!evals/phase3_codeswitch/ground_truth.json
