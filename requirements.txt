# Core
python-dotenv>=1.0.0
jupyter>=1.0.0
ipykernel>=6.29.0

# Image / PDF handling
Pillow>=10.0.0
pdf2image>=1.17.0    # needs poppler installed at the OS level
matplotlib>=3.8.0

# ML / Embeddings
torch>=2.2.0
transformers>=4.45.0
sentence-transformers>=3.0.0   # easy CLIP wrapper for nb 02
colpali-engine>=0.3.4          # ColPali model + processor

# Vector search (small, in-memory for the lab — production would use Milvus/Vespa)
numpy>=1.26.0

# VLM API
openai>=1.40.0
pydantic>=2.5.0

# Multilingual baseline (nb05)
pytesseract>=0.3.10  # needs `tesseract` + lang packs installed at OS level

# Voice — Phase 3
faster-whisper>=1.0.0   # CTranslate2-quantized Whisper, fast on CPU
soundfile>=0.12.1
jiwer>=3.0.4            # CER / WER metrics for nb07

# Video — Phase 4
yt-dlp>=2024.10.0       # downloads from YouTube + Bilibili (and many others)
google-genai>=0.3.0     # nb10 Gemini native-video comparison (optional)
